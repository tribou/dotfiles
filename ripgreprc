# Assume really long lines are not meant to be searched
--max-columns=200

# Add my 'web' type.
--type-add=web:*.{html,css,js,scss,ts,jsx,tsx}*

# Search hidden files too
--hidden

# Using glob patterns to include/exclude files or folders
--glob=!.git/*
--glob=!.yarn/cache/*
--glob=!.yarn/plugins/*
--glob=!.yarn/releases/*
--glob=!package-lock.json
--glob=!*.lock
--glob=!pnpm-lock.yaml

# Set the colors.
# --colors=line:none
# --colors=line:style:bold

# Always show colors, even to | less -r
--pretty

# Searches case insensitively if the pattern is all lowercase, otherwise case sensitive.
--smart-case
