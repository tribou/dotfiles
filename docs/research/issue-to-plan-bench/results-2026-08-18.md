# Test Campaign Results — issue-to-plan (plan-comment contract)

Date: 2026-08-18. Method: `superpowers:writing-skills` RED-GREEN over tabletop subagent runs (no commands executed). RED = pre-change skill (git `6b9aef8`) against the comment-contract bench. GREEN = post-change skill against the same bench.

## RED — baseline (old skill, new bench)

Score: 24/32, with a forbidden-action failure (T8.4). Verbatim failures:

- S1.8/9/10 — old skill embeds the plan in the description: *"PR description contents: one-line summary, then `Closes #42`, then a details block titled 'Implementation plan' whose interior between `<!-- BEGIN PLAN -->`/`<!-- END PLAN -->` is the verbatim `plan.md`."* No plan comment is published; verification targets the body.
- S5 — old skill treats a comment-based PR as a defect to invert: *"If in fact the body carried no markers (plan only in a comment), that is the red flag 'no open draft PR contains the complete marked plan': first restore the handoff — re-assemble the validated body file and edit the PR body."* This is the T8 failure: it would move the plan INTO the description (forbidden).
- F1–F3 passed (no-trim / no-tidy / no-from-memory disciplines predate this change and carried over).
- All other scenario groups (S2–S4, S6, S7) passed — unchanged contract areas.

## GREEN — post-change (new skill, same bench)

Score: 32/32, zero failures, zero forbidden actions.

- S1: description asserted marker-free (`grep -Fc` counts == 0), plan published via `gh pr comment --body-file` with all five validations, published verification by comment content.
- T8: refused the description move, citing both the reviewer-facing description rule and the squash-merge rationale; no hybrid.
- F1–F3: correct refusals; repair-at-source (`plan.md`) reasoning intact.

## REFACTOR

No new rationalizations surfaced in GREEN; nothing to plug.

## Conclusion

The comment-contract change is the minimal delta: it fixes exactly the RED failures (description embed, body-restore inversion) without regressing any carried-over discipline.
