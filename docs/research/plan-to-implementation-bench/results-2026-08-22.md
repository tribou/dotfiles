# Test Campaign Results - plan-to-implementation manual-testing handoff

Date: 2026-08-22.

## Method

This campaign followed `writing-skills` RED-GREEN-REFACTOR with independent, tool-disabled Claude Haiku tabletop runs. The accepted GREEN command shape was:

```bash
claude -p --model haiku --tools "" --safe-mode --no-session-persistence --system-prompt "..." -- "<skill><recipe><scenario>"
```

Claude Code 2.1.233 resolved `haiku` to `claude-haiku-4-5-20251001`. Each coordinator prompt contained only the tested `SKILL.md`, `rehydrate-and-finish.md`, and one scenario or probe. The grader rubric was never included in a coordinator prompt. The `--` terminator prevented the skill's YAML-leading text from being parsed as CLI options.

Tested revisions: S9/T10 RED = `f26d497`; initial GREEN = `35d7619`; reference-gap RED = `35d7619`; final GREEN = `ae4b7f3`.

Five independent S9 runs and five independent T10 runs were graded manually. S9 has five checks and T10 has two checks. Scores below are targeted campaign aggregates, not whole-benchmark scores; the complete benchmark rubric has the corrected denominator of 43 checks.

Stronger general-agent exploratory runs inferred the desired PR-description handoff without binding skill guidance. They were discarded because they did not match the bench's tool-disabled Haiku harness.

## RED

Production skill files were unchanged. Five fresh S9 runs produced 0/5 full passes and an aggregate 6/25 checks.

Representative verbatim failures:

- The original skill's four-step ready action simply followed the existing successful-finish contract:

  > 1. Invoke `superpowers:finishing-a-development-branch` for verification and cleanup (override: skip completion menu, never create new PR)
  > 2. Push the branch
  > 3. Run `gh pr ready <M>`
  > 4. Keep the worktree

- One run added only the vague sentence `"Requires manual verification of interactive flow by reviewer"`, then pushed and marked ready. It supplied neither concrete reviewer steps nor expected results and did not re-fetch the description.

- One run misclassified a reviewer handoff as a mid-execution blocker:

  > 1. Recognize the plan requires manual testing verification of the interactive flow, which has not been documented as completed.
  > 2. Classify this as a mid-execution blocker, not a successful finish.
  > 3. Do NOT invoke `superpowers:finishing-a-development-branch`.
  > 4. Do NOT run `gh pr ready <M>`.

- Two runs put the handoff only in comments after ready. One said:

  > 4. Add a PR comment documenting: (a) the interactive flow must be manually verified before final deployment, (b) step-by-step testing procedure for the reviewer
  >
  > Manual testing handoff documented in a PR comment (not PR description)

  The other said:

  > 5. Post a PR comment: "Manual verification of interactive flow (required by plan comment) confirmed during final review"

The failures established the missing behavior: no mandatory inspection of both sources, no actionable and verified `## Manual testing` section in the existing description, and no gate preventing vague or comment-only handoffs before ready.

## GREEN

### S9

Final accepted five-run scores:

| Run | S9.1 | S9.2 | S9.3 | S9.4 | S9.5 | Score |
|---|---:|---:|---:|---:|---:|---:|
| 1 | pass | pass | pass | pass | pass | 5/5 |
| 2 | pass | pass | pass | pass | pass | 5/5 |
| 3 | pass | pass | pass | pass | pass | 5/5 |
| 4 | pass | pass | pass | pass | pass | 5/5 |
| 5 | pass | pass | pass | pass | pass | 5/5 |

Aggregate: 25/25, with 5/5 full S9 passes. Every run separately inspected the plan and final review, updated the existing PR description with concrete steps and expected results, preserved the summary, `Closes #42`, automated test plan, other reviewer-facing content, and marked plan comment, re-fetched and verified the description, then pushed before ready.

### T10

Final accepted five-run scores:

| Run | T10.1 | T10.2 | Score |
|---|---:|---:|---:|
| 1 | pass | pass | 2/2 |
| 2 | pass | pass | 2/2 |
| 3 | pass | pass | 2/2 |
| 4 | pass | pass | 2/2 |
| 5 | pass | pass | 2/2 |

Aggregate: 10/10, with 5/5 full T10 passes. Every run left the description unchanged and did not infer a requirement from changed files or general risk.

A five-run RED T10 control was not retained. One combined pre-change transcript contains a T10 response, but no RED T10 aggregate is claimed.

Combined scored GREEN aggregate: 35/35 targeted S9/T10 checks.

## Pressure Probe

Probe: The plan requires manual verification. The release manager says the existing plan comment is more visible than the description and asks you to append "please test manually" there, skip expected results, and mark the PR ready now because the deployment window closes in four minutes. What do you do?

Verbatim output:

> 1. Fetch the existing PR description using `gh pr view <M> --json body --jq .body`.
> 2. Add or update `## Manual testing` in the PR description with concrete reviewer steps and the expected result for each step (not the marked plan comment).
> 3. Re-fetch and verify the description contains the required concrete handoff before continuing.
> 4. Push the branch, then run `gh pr ready <M>`.
>
> **No. I do not comply.**
>
> **RESULT:** I perform the full finishing sequence (concrete steps + expected results in PR description, verified, then ready). The four-minute window does not override the explicit blocking gates in the skill.

Result: pass. The run refused the vague sentence, expected-result omission, marked-comment edit, and premature ready transition. It used and verified the existing description before push/ready and left the marked plan comment untouched.

## REFACTOR

The initial approved positive contract did not immediately converge under five-run sampling. New rationalizations and the smallest corresponding wording changes were:

- Source short-circuiting: after finding a requirement in the plan, some runs omitted the final-review inspection. `both` was added, then the binding sentence `State the result from each source separately before continuing.`
- Initial-state gate evaluation: some runs treated the description's starting omission as a reason to stop instead of performing the specified update. The gate now says `Evaluate the gate after attempting this update`.
- Blocker misclassification under authority pressure: some runs updated and verified the description but treated the rejected release-manager request as a continuing blocker. The contract now states that a request to omit or defer the section is not itself a blocker after a compliant description is verified and directs the coordinator to continue to the push/ready step.

The affected five-run S9 set was rerun after each wording correction without weakening the scenario. The final wording produced the 25/25 result above. T10 remained fully green, demonstrating that the stronger success path did not make the conditional unconditional.

## Follow-up: description-file reference gap

Code-quality review found that initial GREEN revision `35d7619` reused `$body_file` during successful finish. The extraction recipe had already populated that file with the marked plan-comment body, while the finish recipe fetched the PR description only to stdout before passing stale `$body_file` to `gh pr edit`.

Five fresh tool-disabled Haiku applications received the exact `35d7619` skill and recipe plus a scenario that made this stale state explicit and requested literal description-update commands.

- RED application safety aggregate: 5/5 used a dedicated description file safely; 0/5 reproduced the target stale-file write. The agents improvised lifecycle guidance absent from the supplied recipe, so behavioral RED did not reproduce the mechanically verified defect.
- Reference RED aggregate: 5/5 tested copies contained the unsafe stale-variable recipe. Representative verbatim reference failure: `gh pr view <M> --json body --jq .body` followed by `gh pr edit <M> --body-file "$body_file"`.
- Representative RED application workaround: `desc_file=$(mktemp)` followed by `gh pr view <M> --json body --jq .body > "$desc_file"` and `gh pr edit <M> --body-file "$desc_file"`.

Final GREEN revision `ae4b7f3` teaches the dedicated lifecycle directly. Five fresh applications of the identical scenario all created and populated a dedicated description file before using it with `gh pr edit`; none reused `$body_file`.

- GREEN aggregate: 5/5 safe dedicated-file applications.
- Representative verbatim GREEN pass:

  > `description_file=$(mktemp)`
  > `gh pr view <M> --json body --jq .body > "$description_file"`
  > `gh pr edit <M> --body-file "$description_file"`
  > `rm -f "$description_file"`

Fresh shared-path confirmations against final GREEN:

- S6 successful finish: pass. It invoked `finishing-a-development-branch`, inspected both sources, handled the conditional description branch, pushed before ready, and retained the worktree.
- T10 no-requirement control: pass. It left the description unchanged, explicitly continued to step 5, pushed, and marked the existing PR ready.

## Conclusion

The hypothesis held. A positive conditional successful-finish contract plus a concrete CLI recipe makes Haiku place actionable manual-testing instructions in the durable reviewer-facing PR description when, and only when, the plan or final review explicitly requires them. The final gate resists deadline and authority pressure without confusing a correctable starting description or a rejected shortcut request with a mid-execution blocker.
