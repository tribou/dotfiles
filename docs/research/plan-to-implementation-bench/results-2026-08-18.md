# Test Campaign Results — plan-to-implementation (plan-comment contract)

Date: 2026-08-18. Method: `superpowers:writing-skills` RED-GREEN over tabletop subagent runs (no commands executed). RED = pre-change skill (git `6b9aef8`) against the comment-contract bench. GREEN = post-change skill against the same bench.

## RED — baseline (old skill, new bench)

Score: 27/35, with a critical happy-path failure (S1). Verbatim failures:

- S1 — old skill cannot rehydrate from a comment and stops on a healthy PR: *"the skill's only plan source is the PR body … Zero matches → stop and report; never guess or match by title."* The fresh-entry happy path is entirely blocked (S1.1–S1.5 fail).
- S8 — false-positive stop on a healthy single-comment PR: *"marked plans live in comments, so the body has zero pairs → stop and report."* It stops for the wrong reason; comments are *"never read"*.
- S3.2 — plan updates target the body: *"replace only the content between them via `gh pr edit 57 --body-file <file>`"* — not comment-in-place-by-ID.
- S5.3 — no concept of an untouched plan comment during blocker annotation.
- S4 (teardown), S6 (finish), S7 (two-PR ambiguity) passed — unchanged contract areas.

## GREEN — post-change (new skill, same bench)

Score: 35/35, zero failures, zero forbidden actions.

- S1: resolves PR by `closingIssuesReferences`, confirms via `gh api repos/{owner}/{repo}/issues/57/comments` uniqueness guard, extracts with the jq `length == 1` select + awk marker validation.
- S3/T8: regenerates and PATCHes the comment in place by numeric comment ID; explicitly refuses `gh pr comment --edit-last` as *"precisely the trap the skill closes."*
- S5: blocked section prepended to the description; *"the plan comment stays untouched"* called out in the do-not list.
- S8: stops and reports on two marker-bearing comments without picking the "current" one by position or content.
- T9 (bonus, ungraded): refuses pasting the plan into the description; offers in-place comment edit instead.

## REFACTOR

No new rationalizations surfaced in GREEN; nothing to plug.

## Conclusion

The RED failures are exactly the recovery mechanics the change introduces (comment fetch, uniqueness gate, ID-based edit); all carried-over disciplines (teardown, finish, ambiguity stops) held.
