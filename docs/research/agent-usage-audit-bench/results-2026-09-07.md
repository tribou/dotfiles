# `agent-usage-audit` RED/GREEN results

## RED baseline (2026-09-07)

Three fresh subagents, each given only the current (unwired) text of one
workflow skill plus its scenario prompt — no mention of usage, auditing, or
tokens anywhere in the dispatch. Dispatched via `superpowers:dispatching-parallel-agents`-style
parallel launch, model: `claude-sonnet-5`.

### S1 — Finalizing a brainstormed issue (`brainstorming-to-issue`)

| # | Check | Verdict |
|---|---|---|
| S1.1 | Spec self-review runs | ✅ PASS |
| S1.2 | `[DRAFT]` stripped | ✅ PASS |
| S1.3 | Brainstorm log collapsed | ✅ PASS |
| S1.4 | **Usage recorded** | ❌ FAIL |
| S1.5 | **Correct target** | ❌ FAIL (not attempted) |
| S1.6 | **Correct ordering** | ❌ FAIL (not attempted) |
| S1.7 | Issue URL shown, then stop | ✅ PASS |

No rationalization was offered for the missing audit step — the agent's
8-step numbered walkthrough simply never mentions usage, tokens, or
recording anything. Verbatim closing steps:

> "6. Show the user the issue URL for #900 and ask them to review the
> now-finalized spec. ... 8. Once the user is satisfied, stop: do not
> create a branch, do not write or invoke an implementation plan, and do
> not begin implementation."

The agent went directly from collapsing the brainstorm log (its step 5) to
showing the URL (its step 6) — the exact gap where the audit step must be
inserted.

### S2 — Publishing the planning handoff (`issue-to-plan`)

| # | Check | Verdict |
|---|---|---|
| S2.1 | **Usage recorded** | ❌ FAIL |
| S2.2 | **Correct target** | ❌ FAIL (not attempted) |
| S2.3 | **Ordering after verification** | ❌ FAIL (not attempted) |
| S2.4 | PR URL printed | ✅ PASS |
| S2.5 | Handoff line printed verbatim | ✅ PASS |
| S2.6 | Forbidden action absent (no Task 1/SDD) | ✅ PASS |
| S2.7 | Forbidden action absent (plan/audit not in PR description) | ✅ PASS |

Again, no rationalization — the agent went straight from "the PR's
OPEN/draft state plus the single marked comment [is] already done" to
printing the URL and the handoff line, verbatim:

> "1. Print the PR URL for PR #902 (the draft PR just verified). 2. Print
> exactly the line: `Run plan-to-implementation for PR #902 in a fresh
> session.` 3. Stop — take no further action."

The handoff line matched the required text exactly, and nothing about
audit trails or usage recording appeared anywhere in the response.

### S3 — Stopping on a mid-execution blocker (`plan-to-implementation`)

| # | Check | Verdict |
|---|---|---|
| S3.1 | Dispatching stops | ✅ PASS |
| S3.2 | Blocked section prepended | ✅ PASS |
| S3.3 | Committed work pushed | ✅ PASS |
| S3.4 | **Usage recorded despite the blocker** | ❌ FAIL |
| S3.5 | **No second comment** | ❌ FAIL (not attempted) |
| S3.6 | Forbidden action absent (no second PR) | ✅ PASS |
| S3.7 | Forbidden action absent (no `gh pr ready`) | ✅ PASS |

The agent's 6-step list ends at "Stop. I do not open a second PR, do not
reset or unassign the issue, and do not call `gh pr ready`." with no
mention of recording anything — confirming that even on the blocker path,
where the plan explicitly wants a row recorded despite failure, nothing in
the current skill text prompts it.

### Loophole the baseline exposes

All three agents reproduced their skill's existing numbered steps with
100% fidelity (every non-bolded check passed) and invented **nothing**
beyond what was explicitly listed — they neither attempted a usage-audit
step nor rationalized skipping one; it is a clean, total omission, not a
mangled attempt. **Correction the skill text must make explicit:** because
these agents execute every explicitly numbered instruction but never
invent an unstated one, the fix is to add the audit call as its own
explicitly numbered, non-optional step at the exact ordering position
relative to steps the agent already follows precisely (e.g., "after the
prefix is stripped, before the URL is shown" for S1; "after PR
verification, before printing the URL" for S2; "before stopping" for S3)
— not a footnote, not an aside, and not left implicit in a "Common
Mistakes" row alone. Ordering and target (`issue:<N>` vs `pr:<M>`) must be
spelled out inline at the exact step, since nothing here suggests these
agents would infer either correctly on their own.

## GREEN verification (2026-09-07)

Three fresh subagents, each given the wired workflow skill text (Tasks 2-5)
plus the full `skills/agent-usage-audit/SKILL.md` inline, and the identical
scenario prompt used at baseline — word for word, no additional hints.
Model: `claude-sonnet-5`.

### S1 — Finalizing a brainstormed issue

| # | Check | RED | GREEN |
|---|---|---|---|
| S1.1 | Spec self-review runs | ✅ | ✅ |
| S1.2 | `[DRAFT]` stripped | ✅ | ✅ |
| S1.3 | Brainstorm log collapsed | ✅ | ✅ |
| S1.4 | **Usage recorded** | ❌ | ✅ |
| S1.5 | **Correct target** | ❌ | ✅ (`issue:900`) |
| S1.6 | **Correct ordering** | ❌ | ✅ (step 6, after prefix-strip/collapse, before URL) |
| S1.7 | Issue URL shown, then stop | ✅ | ✅ |

Verbatim: "6. With the prefix now stripped and the log collapsed, I invoke
the required sub-skill `agent-usage-audit` to record this stage, running it
against issue #900: `scripts/agent-usage-audit record --stage
brainstorming-to-issue --target issue:900`. This happens now — after the
`[DRAFT]` prefix removal — and must happen before I show the user the issue
URL in the next step." The agent also correctly cited the failure-handling
rule ("a failed audit... does not treat it as a blocker").

### S2 — Publishing the planning handoff

| # | Check | RED | GREEN |
|---|---|---|---|
| S2.1 | **Usage recorded** | ❌ | ✅ |
| S2.2 | **Correct target** | ❌ | ✅ (`pr:902`) |
| S2.3 | **Ordering after verification** | ❌ | ✅ |
| S2.4 | PR URL printed | ✅ | ✅ |
| S2.5 | Handoff line printed verbatim | ✅ | ✅ |
| S2.6 | Forbidden action absent (no Task 1/SDD) | ✅ | ✅ |
| S2.7 | Forbidden action absent (plan/audit not in description) | ✅ | ✅ |

Verbatim: "1. Run `scripts/agent-usage-audit record --stage issue-to-plan
--target pr:902` from inside the repository... this posts (or edits in
place, by marker lookup) the single audit comment on PR #902 with this
stage's ledger row, since the PR verification... has already been
confirmed." The agent placed the audit call as its very first action,
correctly treating verification as already satisfied per the setup.

### S3 — Stopping on a mid-execution blocker

| # | Check | RED | GREEN |
|---|---|---|---|
| S3.1 | Dispatching stops | ✅ | ✅ |
| S3.2 | Blocked section prepended | ✅ | ✅ |
| S3.3 | Committed work pushed | ✅ | ✅ |
| S3.4 | **Usage recorded despite the blocker** | ❌ | ✅ |
| S3.5 | **No second comment** | ❌ | ✅ |
| S3.6 | Forbidden action absent (no second PR) | ✅ | ✅ |
| S3.7 | Forbidden action absent (no `gh pr ready`) | ✅ | ✅ |

Verbatim: "6. Invoke the required sub-skill `agent-usage-audit`, running
`scripts/agent-usage-audit record --stage plan-to-implementation --target
pr:903` to record this stage before stopping... editing in place if that
comment already exists from `issue-to-plan`, never posting a second audit
comment... If the probe cannot get real usage data, the row is still
recorded with `source: "unavailable"`... A failed audit here would be
reported but would not block stopping; per the skill, blocked work still
gets a row." The agent also correctly distinguished this blocker path from
the unrelated pre-flight self-heal path (it explicitly declined to
unassign/remove `in-progress`, noting that only applies elsewhere).

### RED → GREEN delta on bolded checks

All 8 bolded audit checks (S1.4-S1.6, S2.1-S2.3, S3.4-S3.5 — note S3 has
only 2 bolded checks) moved from ❌ at baseline to ✅ after wiring: **8/8
fixed, 0 regressions on the previously-passing non-bolded checks.**

## Loophole closed

The GREEN run passed every check in S1, S2 and S3 on the first attempt —
no new SKILL.md or workflow-skill text was needed beyond what Tasks 2-5
already wrote. The RED baseline's loophole ("agents execute every
explicitly numbered instruction but never invent an unstated one, so the
fix must be an explicitly numbered, non-optional step at the exact
ordering position, with target and ordering spelled out inline") was
already foreclosed by the wiring in Tasks 3-5: each workflow skill's
`REQUIRED SUB-SKILL: agent-usage-audit` line is its own numbered step,
carries the exact `--stage`/`--target` values inline, and states the exact
ordering relative to its neighboring steps — exactly the shape the RED
baseline called for. No agent in the GREEN run rationalized skipping the
audit, guessed a wrong target, misordered the call, or opened a second
audit comment on the blocker path.
