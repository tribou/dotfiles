name: ubuntu-dotfiles-tests

# Run this workflow every time a new commit pushed to your repository
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Run bash function tests
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          submodules: true

      - name: Install and verify mise packages and tools
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.local/bin" "$HOME/.config/mise"
          curl -fsSL https://mise.run | MISE_INSTALL_PATH="$HOME/.local/bin/mise" sh
          ln -sfn "$GITHUB_WORKSPACE/mise-config.toml" "$HOME/.config/mise/config.toml"
          "$HOME/.local/bin/mise" bootstrap packages apply --yes
          "$HOME/.local/bin/mise" install --yes
          MISE_BIN="$HOME/.local/bin/mise" ./scripts/verify_mise_tools.sh

      - name: Install just
        uses: extractions/setup-just@v3

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Run bash function tests
        run: just test-unit

      - name: Cache CI dependencies
        uses: actions/cache@v4
        with:
          path: .ci-cache
          key: ci-cache-${{ runner.os }}-${{ hashFiles('init.vim', 'mise-config.toml', 'roles/dotfiles/tasks/**', 'Dockerfile') }}
          restore-keys: |
            ci-cache-${{ runner.os }}-

      - name: Run Docker environment tests
        # Homebrew installs ~35 packages here (~10min) but nothing in goss.yaml
        # or the bats integration tests actually verifies any of them — every
        # check they run is satisfied by apt/mise/the Dockerfile instead. Both
        # local `just test` and CI skip `brew,rust` by default via docker-compose.yml
        # to keep inner-loop runs fast, while `just test-clean` runs cold without
        # skip-tags to verify the full unskipped installation.
        env:
          DOTFILES_ANSIBLE_EXTRA_ARGS: "--skip-tags brew,rust"
        run: just test

      - name: Fix cache permissions for GitHub Actions cache
        if: always()
        run: sudo chown -R "$USER:$USER" .ci-cache 2>/dev/null || true
